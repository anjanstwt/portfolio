// Placeholder until the contact section is designed. Full-height so the
// island's contact state has a section to react to.
export default function ContactSection() {
    return <section className="min-h-screen w-full bg-ink" aria-label="Contact" />;
}
