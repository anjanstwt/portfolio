import { Satisfy } from "next/font/google";

// Script face for the big one-word titles off the home page (Assets, asset
// and project names). Use `satisfy.className` on the heading itself.
export const satisfy = Satisfy({ subsets: ["latin"], weight: "400", display: "swap" });
